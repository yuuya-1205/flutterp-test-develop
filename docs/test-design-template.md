<!--
テスト設計書テンプレート（test-workflow フェーズ0）

このファイルをコピーして `docs/test-design/<feature>.md` などに配置し、機能ごとに記入する。
起点は docs/requirements-template.md で整理した要件。
記入後、エージェントが .github/skills/test-workflow のフェーズ1以降（テスト実装→検証→PR→CI→マージ）を進める。

- 関連 skill: test-workflow / bloc / bloc-state / bloc-test
- ゲート: 観点カバレッジ 100% / 計画ケース数の確定
- 目標カバレッジ: ライン・ブランチとも 100%（CI で強制）
-->

# テスト設計書: <対象>

| 項目 | 内容 |
| --- | --- |
| 対象 | 例: `CounterBloc` / `CounterPage` |
| 種別 | 単体（Bloc）/ Widget（UI） |
| 対応要件 | 例: `docs/requirements/counter.md` |
| 作成日 / 記入者 | YYYY-MM-DD / |
| ステータス | ドラフト / レビュー中 / 確定 |

## 1. テスト対象と範囲

<!-- 何をテストするか。ロジック（Bloc）と UI（Widget）のどちらか／両方かを明記。 -->

- 対象: ...
- 範囲に含む: ...
- 範囲に含まない: ...

## 2. テスト観点の洗い出し

> 4カテゴリを必ず埋める。埋まらない観点は「該当なし」と理由を書く（観点カバレッジ 100% が設計ゲート）。

| 観点カテゴリ | 観点（テストで確認したいこと） | カバー状況 |
| --- | --- | --- |
| 正常系 | 例: 初期状態は count = 0 | ✅ |
| 正常系 | 例: increment で count が 1 増える | ✅ |
| 異常系 | 例: 不正な操作時の扱い | ✅ / 該当なし（理由） |
| 境界値 | 例: count = 0 で decrement しても負にならない | ✅ |
| 状態遷移 | 例: increment→decrement で元の値に戻る | ✅ |

📊 観点カバレッジ = カバー観点数 / 全観点数 = **___ / ___ = ___%**（ゲート: 100%）

## 3. テストケース一覧

> 1ケース＝目的1つ。テスト名は「期待結果」を表す文にする（bloc-test 準拠）。

| ID | 観点 | 前提 (Given) | 操作 (When) | 期待結果 (Then) |
| --- | --- | --- | --- | --- |
| TC-01 | 正常系 | 初期状態 | Bloc を生成 | state.count == 0 |
| TC-02 | 正常系 | count = 0 | `increment()` | state.count == 1 |
| TC-03 | 境界値 | count = 0 | `decrement()` | state.count == 0（負にしない） |
| TC-04 | 状態遷移 | count = 0 | increment→decrement | state.count == 0 |
| TC-05 | UI | Page 表示 | 描画 | count の初期値が表示される |

📊 計画テストケース数: **___ 件**
（内訳 — 正常系: __ / 異常系: __ / 境界値: __ / 状態遷移: __ / UI: __）

## 4. テストデータ / 前提条件

<!-- 共通の初期状態、モック、フィクスチャなど。 -->

- ...

## 5. 実装マッピング（フェーズ1への引き継ぎ）

| ケース ID | テスト種別 | 配置予定ファイル |
| --- | --- | --- |
| TC-01..04 | 単体（bloc_test） | `test/counter/counter_bloc_test.dart` |
| TC-05 | Widget | `test/counter/counter_page_test.dart` |

## 6. レビューチェック（設計ゲート）

- [ ] 4観点（正常系/異常系/境界値/状態遷移）に漏れがない
- [ ] 各ケースが「期待結果」で命名できている
- [ ] 計画ケース数が確定している
- [ ] 対応する要件（受け入れ条件）をすべて満たしている
